---
description: Build the native C++ client using the Makefile wrapper.
---

1. Run the build command:
   ```bash
   make build-native
   ```
2. Monitor output for errors or warnings.
   - If the build fails, investigate the specific compiler error.
   - **Do not ignore warnings.** Attempt to fix them if they are introduced by recent changes.
